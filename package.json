{
  "name": "@storybook-community/storybook-addon-mock-root",
  "description": "A monorepo for storybook-addon-mock",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn run addon:build && yarn run docs:build",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "test": "yarn workspaces run test",
    "ci": "yarn run lint && yarn run test && yarn run addon:build && yarn run docs:build",
    "addon:build": "yarn workspace @storybook-community/storybook-addon-mock run build",
    "docs:start": "yarn workspace @storybook-community/storybook-addon-mock-docs run storybook",
    "docs:build": "yarn workspace @storybook-community/storybook-addon-mock-docs run build",
    "dedup": "yarn-deduplicate"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nutboltu/storybook-addon-mock.git"
  },
  "keywords": [
    "storybook",
    "storybook-community",
    "mock",
    "http-mock",
    "fetch-mock",
    "xhr-mock",
    "mock-data",
    "storybook-addon",
    "data-state",
    "storybook-mock-request",
    "storybook-mock-fetch",
    "mock-request"
  ],
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/eslint-parser": "^7.21.3",
    "@eslint/compat": "^2.0.0",
    "@eslint/js": "^9.39.1",
    "@typescript-eslint/eslint-plugin": "^8.47.0",
    "@typescript-eslint/parser": "^8.47.0",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-jsx-a11y": "^6.7.1",
    "eslint-plugin-prettier": "^5.5.4",
    "eslint-plugin-react": "^7.32.2",
    "globals": "^16.5.0",
    "jest": "^30.2.0",
    "prettier": "^3.6.2",
    "prop-types": "^15.8.1",
    "typescript-eslint": "^8.47.0",
    "webpack": "^5.94.0",
    "yarn-deduplicate": "^6.0.1"
  }
}
